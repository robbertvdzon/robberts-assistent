spring:
  application:
    name: robberts-assistent-backend
  main:
    banner-mode: "off"
  servlet:
    multipart:
      # Moestuin-foto's kunnen enkele MB zijn; ruimer dan de default 1MB/10MB.
      max-file-size: 15MB
      max-request-size: 60MB
server:
  port: ${PORT:8080}
  error:
    include-message: always
