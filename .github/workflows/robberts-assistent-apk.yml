name: Build Robberts Assistent APK

# Bij elke push naar main bouwen we een release-APK en publiceren die als
# downloadbare GitHub Release, zodat de app zonder Play Store of lokale
# build-omgeving te installeren is.
on:
  push:
    branches: [main]
    paths:
      - 'robberts_assistent/**'
      - '.github/workflows/robberts-assistent-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

env:
  # Backend-URL waar de app tegen praat. Moet overeenkomen met waar
  # robberts-assistent-backend + de web-frontend uiteindelijk draaien.
  API_BASE_URL: https://robberts-assistent.vdzonsoftware.nl

jobs:
  build-apk:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: robberts_assistent
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Run tests
        run: flutter test

      - name: Release-keystore klaarzetten
        working-directory: robberts_assistent/android
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
        run: |
          set -euo pipefail
          echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > app/robberts-assistent-release.keystore
          cat > keystore.properties <<EOF
          storeFile=robberts-assistent-release.keystore
          storePassword=${ANDROID_KEYSTORE_PASSWORD}
          keyAlias=${ANDROID_KEY_ALIAS}
          keyPassword=${ANDROID_KEYSTORE_PASSWORD}
          EOF

      - name: Build release APK
        run: |
          flutter build apk --release \
            --dart-define=API_BASE_URL="${{ env.API_BASE_URL }}" \
            --dart-define=GOOGLE_CLIENT_ID="${{ vars.GOOGLE_CLIENT_ID }}"

      - name: Publish APK as GitHub Release
        # Vaste tag (i.p.v. een oplopend buildnummer): elke build overschrijft dezelfde
        # release, zodat er één stabiele downloadlink blijft bestaan i.p.v. een steeds
        # groeiende, door-elkaar-gesorteerde lijst op de releases-pagina.
        uses: softprops/action-gh-release@v2
        with:
          tag_name: robberts-assistent-latest
          name: "Robberts Assistent APK (laatste build)"
          body: |
            Automatisch gebouwde release-APK van de Robberts Assistent-app — wordt bij elke
            build overschreven, dit is dus altijd de nieuwste versie (build ${{ github.run_number }},
            commit ${{ github.sha }}).
            Download `app-release.apk` hieronder en installeer op een Android-toestel
            (sta "installeren uit onbekende bron" toe).

            Backend: `${{ env.API_BASE_URL }}`
          files: robberts_assistent/build/app/outputs/flutter-apk/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
